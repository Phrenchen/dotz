export interface DialogProperties{
    dialogWidth:number;
    dialogHeight:number;
    autoCloseInSeconds:number;           // -1 = wait for user input to close
    txtTitle:string;
    txtDescription:string;
}